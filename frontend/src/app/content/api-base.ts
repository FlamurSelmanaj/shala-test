/**
 * Base URL of the real backend (Express + MySQL, see `backend/`).
 * Update this before a production build — this repo has no Angular
 * environment-file setup, so this is the one place to change for a
 * GoDaddy (or any other) deployment.
 */
export const API_BASE = 'http://localhost:3000';
